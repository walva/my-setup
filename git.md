```
git config core.sshCommand "ssh -i ~/.ssh/otherproject/id_rsa -F /dev/null"
```

```
ssh-agent bash -c 'ssh-add ~/.ssh/id_rsa; git clone git@github.com:S****/project.git'
```

```
git config core.sshCommand "ssh -i ~/.ssh/otherproject/id_rsa -F /dev/null"
```

